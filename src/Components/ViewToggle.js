import React from "react";

const ViewToggle = ({ currentView, onViewChange }) => {
  // component logic
  return <div>View Toggle</div>;
};

export default ViewToggle; // Make sure this is default export
